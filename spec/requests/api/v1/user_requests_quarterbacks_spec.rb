require "rails_helper"

RSpec.describe "Quarterbacks", :type => :request do

  context "successful requests" do
    it "creates a Widget and redirects to the Widget's page" do
      #TODO
      assert(true)
    end
  end
end